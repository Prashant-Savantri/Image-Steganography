# Image-Steganography
Image Steganography is a C-based project that hides secret text data inside an image using LSB (Least Significant Bit) encoding. It supports encoding and decoding of hidden messages while preserving the visual quality of the image. Developed using C, file handling, pointers, and bitwise operations.
